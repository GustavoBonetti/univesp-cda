{
  "build": {
    "env": {
      "DJANGO_SETTINGS_MODULE": "cda.settings"
    }
  },
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/staticfiles/$1"
    }
  ],
  "handlers": [
    {
      "handler": "wsgi",
      "use": "@vercel/python"
    }
  ]
}